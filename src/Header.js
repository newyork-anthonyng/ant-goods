import { h } from "preact";

function Header() {
    return (
        <header>
            <h1>Ant Goods</h1>
            <div style={{fontSize: 200}}>🐜</div>
        </header>
    );
}

export default Header;